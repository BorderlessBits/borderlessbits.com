name: 🔍 Monitoring & Health Checks [DISABLED]

# ==========================================
# WORKFLOW IS COMPLETELY DISABLED
# ==========================================
# To re-enable monitoring:
# 1. Uncomment the schedule triggers in the 'on' section
# 2. Remove the 'disabled-notice' job
# 3. Uncomment all monitoring jobs below
# ==========================================

on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled'
        required: false
        default: true
        type: boolean

jobs:
  disabled-notice:
    name: ⚠️ Workflow Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Workflow is disabled
        run: |
          echo "⚠️ Monitoring and health checks are currently disabled"
          echo "To re-enable, edit .github/workflows/monitoring.yml"
          echo "" 
          echo "This workflow included:"
          echo "- Uptime monitoring"
          echo "- SSL certificate checks"
          echo "- Performance monitoring (Lighthouse)"
          echo "- DNS resolution checks"
          echo "- Security headers validation"
          echo "- Dependency vulnerability scanning"
          exit 0

# ==========================================
# DISABLED MONITORING CONFIGURATION
# ==========================================
# 
# Original schedule configuration:
# on:
#   schedule:
#     # Every 5 minutes during business hours (9 AM - 6 PM UTC, Mon-Fri)
#     - cron: '*/5 9-18 * * 1-5'
#     # Every 15 minutes outside business hours
#     - cron: '*/15 0-8,19-23 * * 1-5'
#     - cron: '*/15 * * * 0,6'
#   workflow_dispatch:
#     inputs:
#       full_check:
#         description: 'Run full monitoring suite'
#         required: false
#         default: false
#         type: boolean
#
# env:
#   MONITORING_TIMEOUT: 30
#
# Monitoring jobs that were active:
# - uptime-check: Check site availability
# - performance-check: Lighthouse audits
# - ssl-check: SSL certificate validation
# - dns-check: DNS resolution monitoring
# - security-headers-check: Security headers validation
# - dependency-check: npm audit for vulnerabilities
# - alert-handler: Notification handling
#
# Full job definitions are preserved in version control history
# ==========================================